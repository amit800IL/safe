using UnityEngine;

[CreateAssetMenu(menuName = "Level Data/Create breathing level data", fileName = "Breathing level data", order = 0)]
public class LevelDataObject_breathing : LevelDataObject
{
	public Color StartColor;
	public Color EndColor;
}
